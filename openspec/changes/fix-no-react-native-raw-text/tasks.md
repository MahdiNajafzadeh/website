## 1. Preparation

- [x] 1.1 Read `DESIGN.md` and confirm tokens for affected surfaces (`{colors.mute}` `#707072`, `{colors.ink}` `#111111`, `{typography.caption-md}`, `{typography.heading-xl}`, `{rounded.lg}`) and verify `src/app/(app)` exists via `ls src/app/\(app\)` — no `DESIGN.md` edits, no `(payload)` writes
- [x] 1.2 Verify UI registry not needed: run `bunx --bun shadcn@latest search 2>&1 | head -20` and `curl -fsS https://beui.dev/r/registry.json 2>&1 | head -20` to confirm no `@beui/*` component covers this string-only fix; existing `Card`/`Badge`/`Pagination` from `shadcn` remain

## 2. Locale keys — `src/lib/locale.ts`

- [x] 2.1 Add separator and interpolation keys to `src/lib/locale.ts` and verify `bun run generate:types` still type-checks (no Payload change, but `t` types derive from `LocaleKey`): `common.breadcrumbSeparator: "›"`, `common.breadcrumbSeparatorSlash: "/"`, `common.dotSeparator: "·"` (if standalone), `account.userMeta: "{firstName} {lastName} · {phone}"`, `account.profileWithPhone: "{profile} — {phone}"`, `blog.noContent: "No content."`, `blog.emptyTitle: "No articles yet"`, `blog.emptyHint: "Check back soon for new stories."`, `blog.backToHome: "Back to home"`, `blog.articleCount: "{count} {label}"` / `blog.pagination: "Page {page} of {totalPages} · {totalDocs} articles"`, `brands.countLabel: "{count} {label}"`, `categories.brandsLabel: "Brands:"`, `categories.allBrandsIn: "All brands in {name}"`, etc. — verify file imports correctly via `bunx --bun tsc --noEmit --skipLibCheck 2>&1 | head -20`

## 3. Replace raw text with `t()` in `src/app/(app)`

- [x] 3.1 Fix `src/app/(app)/about/page.tsx:42` — import `t` already present, replace `<span aria-hidden>›</span>` with `<span aria-hidden>{t("common.breadcrumbSeparator")}</span>` and verify `biome lint src/app/\(app\)/about/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty
- [x] 3.2 Fix `src/app/(app)/account/page.tsx:58,77,88` — replace breadcrumb `›` with `t("common.breadcrumbSeparator")`, collapse `·` and `—` whitespace-joined lines into single interpolated `t` calls: `t("account.userMeta", { firstName: user.firstName ?? "", lastName: user.lastName ?? "", phone: phoneFormatted })` and `t("account.profileWithPhone", { profile: t("account.profile"), phone: phoneFormatted })` — verify `biome lint src/app/\(app\)/account/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty
- [x] 3.3 Fix `src/app/(app)/blog/[slug]/page.tsx:141-176` — add `import { t } from "@/lib/t"` if missing, replace hardcoded `Home` → `t("common.home")`, `Blog` → `t("blog.title")`, both `›` → `t("common.breadcrumbSeparator")`, and `No content.` → `t("blog.noContent")` — verify `biome lint src/app/\(app\)/blog/\[slug\]/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty
- [x] 3.4 Fix `src/app/(app)/blog/page.tsx:93-222` — add/reuse `t` import, replace `Home` → `t("common.home")`, `Blog` heading → `t("blog.title")`, `/` separator → `t("common.breadcrumbSeparatorSlash")`, `No articles yet`/`Check back soon…`/`Back to home` → `t("blog.emptyTitle")`/`t("blog.emptyHint")`/`t("blog.backToHome")`, and collapse count/pagination whitespace joins into interpolated keys (`t("blog.articleCount", { count: totalDocs.toLocaleString("fa-IR"), label: t(totalDocs===1?"blog.articleSingular":"blog.articlePlural") })` or similar, and `t("blog.pagination", { page, totalPages, totalDocs })`) — verify `biome lint src/app/\(app\)/blog/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty
- [x] 3.5 Fix `src/app/(app)/brands/page.tsx:37,45` and `src/app/(app)/brands/[slug]/page.tsx:56,60,87,97` — replace `/` separators with `t("common.breadcrumbSeparatorSlash")` and collapse `{count} {t("common.…")}` whitespace joins into `t("brands.countLabel", { count: …, label: t("…") })` / `t("common.countWithLabel", …)` — verify `biome lint src/app/\(app\)/brands/page.tsx src/app/\(app\)/brands/\[slug\]/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty
- [x] 3.6 Fix `src/app/(app)/categories/page.tsx:76,84` and `src/app/(app)/categories/[slug]/page.tsx:63,67,81,87,91,107,118,123,135,178` — add `t` import where missing, replace `/` with `t("common.breadcrumbSeparatorSlash")`, `Brands:` → `t("categories.brandsLabel")`, `All brands in …` → `t("categories.allBrandsIn", { name: category.name })`, `All products`/`only`/`Clear category`/`Browse products`/`ناموجود` etc. with `t()` and collapse whitespace-joined counts — verify `biome lint src/app/\(app\)/categories/page.tsx src/app/\(app\)/categories/\[slug\]/page.tsx --colors=off 2>&1 | grep noReactNativeRawText` is empty

## 4. Verification

- [x] 4.1 Run full lint: `bunx --bun biome lint . --colors=off 2>&1 | grep noReactNativeRawText | wc -l` must be `0` and `bunx --bun biome lint src/app --colors=off 2>&1 | grep noReactNativeRawText | wc -l` must be `0` — capture output
- [x] 4.2 Run build and type check: `bunx --bun tsc --noEmit --skipLibCheck` and `bun run build` succeed with no new errors; `git diff --stat` shows only `src/lib/locale.ts` and `src/app/(app)/**/*.tsx` changes, no `biome.json` or `DESIGN.md` or `(payload)` changes
